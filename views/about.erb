<p>
	short url: <b> <%= short_url %> </b> <span>[<a href='<%= short_url %>'>link</a>]</span>
	<br>
	original url: <a href='<%= url %>'><%= url %></a>
	<br>
	<a href="/">shorten another?</a>
</p>
<p>
	stats:
	<br>
	short url length: <%= short_url.length %> characters 
	<br> 
	original url length: <%= url.length %> characters
	<% if url.length - short_url.length <= 0 %>
		<br>
		You save nothing! You lose! Good day,sir!
	<% else %>
		<br>
		saving <%= url.length - short_url.length %> characters
	<% end %>
	<br>
	views: <%= views %>
</p>